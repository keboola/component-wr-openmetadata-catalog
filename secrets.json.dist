{
  "_comment": "Skeleton for tests/../secrets.json (git-ignored). Copy to `secrets.json` at the repo root and fill REAL values, then record with: uv run python -m keboola.datadirtest scaffold --secrets secrets.json --freeze-time 2026-08-24T12:00:00. Keys only here, NO values. The scaffolder deep-merges this into every test's config at record time and masks the values back out of the committed config.json. See tests/setup/README.md for which passes must OMIT #storage_token (cases 04, 16) or ADD #manage_token (case 19).",
  "parameters": {
    "#bot_token": "",
    "#storage_token": ""
  }
}
