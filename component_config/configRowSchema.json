{
  "type": "object",
  "title": "Source",
  "properties": {
    "scope": {
      "type": "string",
      "title": "Scope",
      "enum": [
        "this_project",
        "all_projects"
      ],
      "default": "this_project",
      "description": "What this row catalogs",
      "options": {
        "enum_titles": [
          "This project",
          "All organization projects"
        ],
        "tooltip": "This project catalogs a single project via a Storage token (or the forwarded host-project token). All organization projects enumerates the org with a Management token and writes every project it finds."
      },
      "propertyOrder": 10
    },
    "#storage_token": {
      "type": "string",
      "title": "Storage Token",
      "format": "password",
      "description": "Read-only Storage token for this project",
      "options": {
        "dependencies": {
          "scope": "this_project"
        },
        "tooltip": "Leave empty to catalog the config's own (host) project via the forwarded token. Required for any other project: create a read-only Storage token in that project and paste it here."
      },
      "propertyOrder": 20
    },
    "#manage_token": {
      "type": "string",
      "title": "Management Token",
      "format": "password",
      "description": "Management API token with manage:storage-tokens scope",
      "options": {
        "dependencies": {
          "scope": "all_projects"
        },
        "tooltip": "Org-wide credential used only to enumerate projects and mint short-lived read-only Storage tokens. Host the config in a locked-down admin project."
      },
      "propertyOrder": 30
    },
    "organization_id": {
      "type": "string",
      "title": "Organization ID",
      "description": "Keboola organization to enumerate",
      "options": {
        "dependencies": {
          "scope": "all_projects"
        },
        "tooltip": "Enumeration is bound to this single organization via GET /manage/organizations/{id}/projects; the component never lists all organizations."
      },
      "propertyOrder": 40
    },
    "project_name_override": {
      "type": "string",
      "title": "Project Name Override (Optional)",
      "description": "Optional human-readable OpenMetadata Database name",
      "options": {
        "dependencies": {
          "scope": "this_project"
        },
        "tooltip": "Defaults to the project name resolved from the token."
      },
      "propertyOrder": 50
    },
    "objects": {
      "type": "object",
      "title": "Objects",
      "description": "Which object families to catalog from this project",
      "propertyOrder": 60,
      "options": {
        "dependencies": {
          "scope": "this_project"
        }
      },
      "properties": {
        "write_buckets": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Buckets",
          "default": true,
          "description": "Catalog buckets, tables, and columns",
          "options": {
            "tooltip": "Catalog buckets; uncheck to skip this family entirely."
          },
          "propertyOrder": 10
        },
        "buckets": {
          "type": "array",
          "title": "Only these buckets",
          "format": "select",
          "uniqueItems": true,
          "description": "Buckets to catalog (empty = all)",
          "items": {
            "type": "string",
            "enum": []
          },
          "options": {
            "dependencies": {
              "write_buckets": true
            },
            "tags": true,
            "async": {
              "label": "Load Buckets",
              "action": "listBuckets",
              "autoload": true,
              "cache": true
            }
          },
          "propertyOrder": 20
        },
        "write_transformations": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Transformations",
          "default": true,
          "description": "SQL / Python transformations",
          "options": {
            "tooltip": "Catalog transformations; uncheck to skip this family entirely."
          },
          "propertyOrder": 30
        },
        "transformations": {
          "type": "array",
          "title": "Only these transformations",
          "format": "select",
          "uniqueItems": true,
          "description": "Transformations to emit as pipelines (empty = all)",
          "items": {
            "type": "string",
            "enum": []
          },
          "options": {
            "dependencies": {
              "write_transformations": true
            },
            "tags": true,
            "async": {
              "label": "Load Transformations",
              "action": "listTransformations",
              "autoload": true,
              "cache": true
            },
            "tooltip": "Only selectable for a single project. An organization-wide row emits every eligible transformation it finds in each project."
          },
          "propertyOrder": 40
        },
        "write_components": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Components",
          "default": true,
          "description": "Extractors, writers, and other component configurations",
          "options": {
            "tooltip": "Catalog non-transformation component configurations; uncheck to skip this family entirely."
          },
          "propertyOrder": 50
        },
        "components": {
          "type": "array",
          "title": "Only these components",
          "format": "select",
          "uniqueItems": true,
          "description": "Components to emit as pipelines (empty = all)",
          "items": {
            "type": "string",
            "enum": []
          },
          "options": {
            "dependencies": {
              "write_components": true
            },
            "tags": true,
            "async": {
              "label": "Load Components",
              "action": "listComponents",
              "autoload": true,
              "cache": true
            },
            "tooltip": "Only selectable for a single project. Covers non-transformation configurations, e.g. a MySQL extractor configuration lives here, not under Transformations."
          },
          "propertyOrder": 60
        },
        "write_flows": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Flows",
          "default": true,
          "description": "Emit flows as pipeline entities",
          "options": {
            "tooltip": "Catalog flows; uncheck to skip this family entirely."
          },
          "propertyOrder": 70
        },
        "flows": {
          "type": "array",
          "title": "Only these flows",
          "format": "select",
          "uniqueItems": true,
          "description": "Flows to emit as pipelines (empty = all)",
          "items": {
            "type": "string",
            "enum": []
          },
          "options": {
            "dependencies": {
              "write_flows": true
            },
            "tags": true,
            "async": {
              "label": "Load Flows",
              "action": "listFlows",
              "autoload": true,
              "cache": true
            },
            "tooltip": "Only selectable for a single project. An organization-wide row emits every eligible flow it finds in each project."
          },
          "propertyOrder": 80
        },
        "write_data_apps": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Data Apps",
          "default": true,
          "description": "Catalog data apps as OpenMetadata dashboards",
          "options": {
            "tooltip": "Catalog data apps; uncheck to skip this family entirely."
          },
          "propertyOrder": 90
        },
        "data_apps": {
          "type": "array",
          "title": "Only these data apps",
          "format": "select",
          "uniqueItems": true,
          "description": "Data apps to catalog as dashboards (empty = all)",
          "items": {
            "type": "string",
            "enum": []
          },
          "options": {
            "dependencies": {
              "write_data_apps": true
            },
            "tags": true,
            "async": {
              "label": "Load Data Apps",
              "action": "listDataApps",
              "autoload": true,
              "cache": true
            },
            "tooltip": "Only selectable for a single project. An organization-wide row emits every eligible data app it finds in each project. Each data app becomes a Dashboard entity (keboola.data-apps) with upstream table lineage from its input mapping."
          },
          "propertyOrder": 100
        }
      }
    },
    "projects": {
      "type": "array",
      "title": "Only these projects",
      "format": "select",
      "uniqueItems": true,
      "description": "Projects to catalog (empty = all organization projects)",
      "items": {
        "type": "string",
        "enum": []
      },
      "options": {
        "dependencies": {
          "scope": "all_projects"
        },
        "tags": true,
        "async": {
          "label": "Load Projects",
          "action": "listProjects",
          "autoload": true,
          "cache": true
        },
        "tooltip": "Every family (buckets, configurations, flows, data apps) is written for each selected project; there is no per-family filter in this scope."
      },
      "propertyOrder": 70
    },
    "advanced": {
      "type": "object",
      "title": "Advanced",
      "propertyOrder": 200,
      "options": {
        "collapsed": true
      },
      "properties": {
        "lineage": {
          "type": "object",
          "title": "Lineage",
          "format": "grid-strict",
          "description": "Which lineage types to emit",
          "propertyOrder": 10,
          "properties": {
            "write_bucket_lineage": {
              "type": "boolean",
              "format": "checkbox",
              "title": "Bucket lineage",
              "default": true,
              "description": "Emit lineage between buckets, aggregated from table lineage",
              "options": {
                "grid_columns": 6,
                "tooltip": "Enabled by default."
              },
              "propertyOrder": 1
            },
            "write_table_lineage": {
              "type": "boolean",
              "format": "checkbox",
              "title": "Table lineage",
              "default": true,
              "description": "Emit declared table-level lineage",
              "options": {
                "grid_columns": 6,
                "tooltip": "Enabled by default."
              },
              "propertyOrder": 2
            },
            "write_column_lineage": {
              "type": "boolean",
              "format": "checkbox",
              "title": "Column lineage",
              "default": true,
              "description": "Emit column-level lineage from transformation SQL",
              "options": {
                "grid_columns": 6,
                "tooltip": "Enabled by default."
              },
              "propertyOrder": 3
            },
            "write_pipeline_lineage": {
              "type": "boolean",
              "format": "checkbox",
              "title": "Pipeline lineage",
              "default": true,
              "description": "Emit lineage between pipelines and the tables they read or write",
              "options": {
                "grid_columns": 6,
                "tooltip": "Enabled by default."
              },
              "propertyOrder": 4
            },
            "write_dashboard_lineage": {
              "type": "boolean",
              "format": "checkbox",
              "title": "Dashboard lineage",
              "default": true,
              "description": "Emit dashboard lineage from data app input mappings",
              "options": {
                "grid_columns": 6,
                "tooltip": "Enabled by default."
              },
              "propertyOrder": 5
            }
          }
        },
        "write_pipeline_status": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Write Pipeline Status",
          "default": true,
          "description": "Emit each pipeline's run history (last job status)",
          "options": {
            "tooltip": "Enabled by default. Applies to the pipeline entities this row emits from Configurations and Flows."
          },
          "propertyOrder": 20
        },
        "full_refresh": {
          "type": "boolean",
          "format": "checkbox",
          "title": "Full Refresh",
          "default": false,
          "description": "Ignore per-bucket digests and re-push everything this run",
          "options": {
            "tooltip": "Disabled by default; incremental digests skip unchanged buckets."
          },
          "propertyOrder": 30
        },
        "merge_mode": {
          "type": "string",
          "title": "Merge Mode",
          "enum": [
            "three_way_merge",
            "keboola_always_wins"
          ],
          "default": "three_way_merge",
          "description": "How to reconcile OpenMetadata edits with Keboola-sourced values",
          "options": {
            "enum_titles": [
              "Three-way merge",
              "Keboola overwrites"
            ],
            "tooltip": "Three-way merge keeps fields a person edited in OpenMetadata and logs the change; Keboola overwrites replaces them."
          },
          "propertyOrder": 40
        },
        "failure_mode": {
          "type": "string",
          "title": "Failure Mode",
          "enum": [
            "collect_and_fail",
            "fail_fast",
            "log_only"
          ],
          "default": "collect_and_fail",
          "description": "How entity failures affect the job outcome",
          "options": {
            "enum_titles": [
              "Collect and fail at end",
              "Fail fast",
              "Log only"
            ],
            "tooltip": "Collect and fail at end records every per-entity failure in the run report and fails the job once all entities are processed. Fail fast stops on the first failure. Log only never fails the job."
          },
          "propertyOrder": 50
        }
      }
    }
  }
}
